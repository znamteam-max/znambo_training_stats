name: ZnamboHealthSync
options:
  deploymentTarget:
    iOS: "17.0"
settings:
  base:
    PRODUCT_BUNDLE_IDENTIFIER: app.znambo.healthsync
    SWIFT_VERSION: "5.9"
targets:
  ZnamboHealthSync:
    type: application
    platform: iOS
    sources:
      - Sources
    info:
      path: Info.plist
    entitlements:
      path: ZnamboHealthSync.entitlements
    settings:
      base:
        INFOPLIST_FILE: Info.plist
        CODE_SIGN_STYLE: Automatic
